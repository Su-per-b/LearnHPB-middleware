################################################################################
# Build Properties
#
source.dir=src
source.java.dir=${source.dir}
source.resource.dir=${source.dir}/resources
source.test.dir=${source.dir}/test

build.dir=build
build.classes.dir=${build.dir}/classes/
build.resource.dir=${build.dir}/resources/
build.cobertura.dir=${build.dir}/cobertura/
build.lib.dir=lib

build.instr.dir=emma_instr
coverage.dir=reports/coverage

target.dir=target

reports.test=reports

conf.dir=conf

# Path to nsis compiler used for creating WIN32 installer
nsiscompiler=/usr/local/bin/makensis

# Launch4j is used for creating the windows .exe that wraps Photovault
# Specify the directory where Launch4j is installed if you want to 
# create a Windows install package
launch4j.dir=/home/harri/download/launch4j

# Location of Findbugs installation (for findbugs task)
findbugs.home=/home/harri/java/findbugs-1.3.5-rc3/